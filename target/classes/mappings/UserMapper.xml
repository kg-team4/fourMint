<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="four.mint.web.common.mapper.UserMapper">
	<!-- <select id="getList" resultType="four.mint.web.user.UserVO">
		<![CDATA[SELECT * FROM tbl_board WHERE bno > 0]]>
	</select> -->
	
	<insert id="insert">
		INSERT INTO member (email_id, password, nickname, phone)
		VALUES (#{email_id}, #{password}, #{nickname}, #{phone})
	</insert>
	
	<!-- <insert id="insertSelectKey">
		<selectKey keyProperty="bno" order="BEFORE" resultType="long">
			SELECT nextval('seq_board')
		</selectKey>
		
		INSERT INTO tbl_board (bno, title, content, writer)
		VALUES (#{bno}, #{title}, #{content}, #{writer})
	</insert>
	
	<select id="read" resultType="org.zerock.domain.BoardVO">
		SELECT * FROM tbl_board WHERE bno = #{bno}
	</select>
	
	<delete id="delete">
		DELETE FROM tbl_board WHERE bno = #{bno}
	</delete>
	
	<update id="update">
		UPDATE tbl_board
		SET
		title = #{title},
		content = #{content},
		writer = #{writer},
		updateDate = now()
		WHERE bno = #{bno}
	</update> -->
</mapper>